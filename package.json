{
  "name": "mds-websites",
  "version": "1.0.0",
  "private": true,
  "description": "MdS Websites — static site with Eleventy-powered blog",
  "scripts": {
    "optimize": "node scripts/optimize-images.js",
    "sitemap": "node scripts/generate-sitemap.js",
    "build": "node scripts/optimize-images.js && eleventy && node scripts/generate-sitemap.js",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "sharp": "^0.33.5"
  }
}
