{
  "name": "mds-websites",
  "version": "1.0.0",
  "private": true,
  "description": "MdS Websites — static site with Eleventy-powered blog",
  "scripts": {
    "optimize": "node scripts/optimize-images.js",
    "build": "node scripts/optimize-images.js && eleventy",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "sharp": "^0.33.5"
  }
}
